includes:
    - ./vendor/larastan/larastan/extension.neon

parameters:

    paths:
        - src

    # The level 9 is the highest level
    level: 8

    ignoreErrors:
        #- '#Unsafe usage of new static#'
        #- '#Cannot call method info\(\) on Illuminate\\Log#'

    excludePaths:
        #- ./*/*/FileToBeExcluded.php